# 04 · 框架优化清单（2026-06-29 多代理审阅 → 对抗式核验）

> 来源：47 代理工作流，35 候选 → 核验后 **26 条确认**（真实且框架级）+ 9 条驳回。
> 标尺：严重度 H/M/L、工作量 S/M/L、是否对齐产品方向（**强化拟人黏性、不要毕业、弱化情感健康守护**）。
> 约定：本机（目标机/E:）只改不跑，**所有改动由运行机（F:\Code\Mirrai）跑 `tsc` + `vitest` 验证后再提交**。
> 先决：工作树尚有未提交 WIP（场景命令/续写引导/源召回）。**建议先让运行机把这批 WIP 落地，再批量执行下面 B1+**，避免大重构与在飞改动冲突。

---

## ✅ 已落地（本机已改，待运行机验证+提交）

- **[13] 关闭毕业 live 触发**（方向收口，P0）
  - 改：`server/routers.ts` `chat.send` 删 477-493 的 `checkGraduationEligibility` 分支，`graduationSuggested` 恒置 `false`。
  - 留：`checkGraduation` query / `graduate` / `declineGraduation` / `awaken`（routers.ts:340-400）与 client 的 `GraduationModal` 暂未动（见 B1-[13b]）。
  - 风险：极低。字段名保留，client 类型不破；`checkGraduationEligibility` import 仍被 340-365 的 query 引用，不会变野。
  - 验证点：`tsc` 通过；网页聊到「灵魂伴侣」不再弹「毕业时刻」。
- **[22] 删 verbose-commands 死模块**
  - 改：删 `server/qq/verbose-commands.ts` + `verbose-commands.test.ts`（全仓仅其自身 test 引用，生产零接线；长回复需求已由 `replyLengthOverride='long'` + `wantsLongerReply` 覆盖）。
  - 风险：极低。
  - 验证点：`tsc`/`vitest` 通过，无悬空 import。
- **[10] getPersonaById 两条 SQL → 一条 LEFT JOIN**（B2 性能头牌）
  - 改：`server/db.ts` `getPersonaById` 用 `personas LEFT JOIN personaRuntimeStates` 单查询替代「先查 personas 再单独查 runtime」，结果喂回原 `mergePersonaRuntimeRow`。签名/返回类型不变，32+ 调用方零改。
  - 行为保真：null db→undefined、无 persona→undefined、无 runtime→原样返回，均与原逻辑一致（getDb 的 null 优雅返回保留在 ensure 之前）。
  - 风险：低。仅 `getPersonaById` 单点；未做「回合内 persona 快照复用」那半（要碰 WIP 的 persona-bridge/text-chat，留 B2 续做）。
  - 验证点：`vitest` persona 相关 + 手测一条 QQ 消息正常；运行机确认 Drizzle leftJoin 嵌套结果 `joined.personas`/`joined.personaRuntimeStates` 取值正确。
- **[18] output-diagnostics 导出/删除分段去手抄副本**
  - 改：`server/social/output-diagnostics.ts` 删本地 `PLAN2_EXPORT_SECTIONS`/`PLAN2_DELETE_SECTIONS`，改 import db.ts 的 canonical `USER_DATA_EXPORT_SECTIONS`/`USER_ACCOUNT_DELETE_SECTIONS`（两份原本逐行一致，已核）。`PLAN2_PERSISTENT_TABLES`/`PLAN2_REQUIRED_MIGRATIONS` 因 db.ts 无对应导出，暂留。
  - 风险：低。值一致、test（:151-152 仅断言含 personaRuntimeStates）仍绿、无循环依赖（db.ts 不引用 output-diagnostics）、无 orphan。
  - 验证点：`vitest output-diagnostics` 绿。

---

## 🔄 与运行机并行 #24/#25/#26 对账（2026-06-29）

> 我做框架审阅期间，运行机并行合并了 #24/#25/#26。逐条核对结论：**这三者没有完整做掉本清单任何一条**——都是同子系统的「行为修复 / 沉浸增强功能」，未做框架重构；其中 #26 还**加重**了部分硬编码、改了续写结构。**重要纠正**：先前口头说「#24 = [2] 已做」是判断错误。

- **[2] 沉浸真相 web 入口**：**未被 #24 修掉**。#24（`b907ba7`「沉浸态统一为单一真相」）修的是 *QQ* 内存污染那个「已退出场景仍出旁白」的 bug——删了 `persona-bridge` 的 `setSceneMode` 单向回灌、把 `immersiveMode` 加成 `SocialPersonaTextChatResult` 字段、bridge 一处算。但 **web 入口（routers.ts）仍不传 immersiveMode**，生成层仍用 `options.immersiveMode`（web→false），**[2] 描述的 web 不一致 + 未下沉 social 共享解析器仍成立**。行号更新：bridge 现 `const immersiveMode = getSceneMode||sceneOverlay`（约 :72-75）、result 多了 `immersiveMode` 字段。
- **[7] / [20] persona-utils 沉浸/系统提示硬编码**：#26（`cc8d4db`）**加重**——新增「设定围栏（锁性格/身材/语气）」「双人旁白视角=敏子」「原著文笔（+ `persona_material/wang-pengze-narrative-style.md`）」，`persona-utils.ts` +56 行。两条不仅仍有效、且更该数据驱动；**行号全部上移，落地前重新定位**。
- **[9] / [23] source-recall 触发词**：#25（`ccdf543`）已从 `SOURCE_RECALL_TRIGGER` **移除 `学校/北京/南京/见面`**（日常高频词误触发原著考据）。框架点（移到 `lifeConfig.sourceTerms` / 统一 Recaller 契约）**未动**，仍有效；但我清单 [9] 引用的词表（含 北京/南京/学校）**已过时，以现状正则为准**。`ambient-proactive` 同时加了防编造约束。
- **[17] keepGoing/dualMode 续写**：#26 **重写**了续写链——新增 `continuation-guidance.ts`、`keepGoingBeats` 多拍、`dual=getDualMode` 传入 guidance、媒体链透传 immersiveMode/dualMode。[17] 所述旧结构（keepGoing 被 continuationCue 旁路）**已变，需对现状代码重新判定**是否仍漏传。
- **[1] 沉浸标志群平铺 4 层 / [5] message-handler 拆分 / [6][15] 文本·媒体链同构**：#24/#26 触及同区文件但**未做框架收敛**，仍有效（#26 后 message-handler 行数更多、文本/媒体链透传字段又增，[15] 漂移更明显）。
- **其余 [3][4][10√][11][12][14][16][18√][19][21][22√][24][25][26][8]**：#24/#25/#26 未触及（[10][18][22] 已由我 #27 落地）。

**净结论**：清单整体仍有效；**无一条被运行机完整做掉**，[7]/[20] 反而更紧迫，多条行号因 #26 变化、落地前需按现状重新定位。

---

## B1 · 方向收口 + 死代码清理（半天，低风险，最对齐方向）

- **[13b] 毕业机制收尾**（H/M）：删 `routers.ts:340-365` 死 query `checkGraduation`、`persona-utils.ts:205` `checkGraduationEligibility` 导出及 routers.ts:37 import；client 删 `GraduationModal` 挂载（`Chat.tsx` import/state/触发/渲染）。验证：`tsc` + 前端构建。
- **[12] 微信端死代码**（H/M）：分三步——①删 db.ts 5 个 wechat binding 死函数（`createWechatBinding`/`getWechatBindingsByUserId`/`getActiveWechatBindingsByPersonaId`/`getWechatBindingByContactId`/`deleteWechatBinding`，生产零调用）+ `server/persona.test.ts` 的 vi.mock 行；②删 `wechat_bot_state` 表 botState 助手 + `wechatBound` overview 字段；③`output-diagnostics.ts` 去微信项。
  - **⚠️ 2026-06-29 核查修正（本机已 grep 证实）**：`wechat_bindings` **表是活的**——`createQqBinding`（db.ts:1946）等 QQ 链路也写这张表（用 `nonQqBindingFilter()` 区分 QQ/非 QQ）。所以**只有那 5 个 wechat 专用函数是死的，表/`nonQqBindingFilter`/`InsertWechatBinding` 不能删**。
  - **⚠️ `tsconfig.node.json` 开了 `noUnusedLocals`+`noUnusedParameters`**：删那 5 个函数后若 `nonQqBindingFilter`/`InsertWechatBinding` 变成无引用，会**直接 tsc 报错**。删完必须在运行机 `tsc` 验证，必要时连带清理 orphan。本机因不能跑 tsc，**本项未动**。

## B2 · 低风险高价值的框架健康（性能 + 契约，不碰行为）

- **[10] persona 重复读 + getPersonaById 两条 SQL**（H/M）：①✅**已落地**（见上「已落地」段）`getPersonaById` 合 LEFT JOIN。②**待做**：回合内 persona 快照复用（bridge 的 `resolveSceneOverlay` 与 text-chat 各读一次 → 传递复用）——**要改 persona-bridge.ts/persona-text-chat.ts（WIP 文件），等 WIP 落地后做**。
- **[11] 每回合全表拉 active memories + 同回合读两遍**（H/M，**仅子项2高价值**）：由 `persona-text-chat` 回合内拉一次活跃记忆快照，分别传 recall 与 consolidation 复用。**核验更正**：recall 末尾 `touchMemoriesByIds` 会改 `lastAccessed`，快照复用要注意不要让 consolidation 读到过期 access 字段（治理只看 title/内容，不看 access，安全）。
- **[16] runtime-request 共享契约接线**（M/S）：`SocialPersonaTextChatOptions` / `SocialPersonaMediaChatOptions` 都 `extends SocialRuntimeRequestBase`，各补专属字段。零风险纯结构。
- **[3] db.ts 按领域拆 repository**（H/L）：按文件内已有 21 个 `// ─── X helpers ───` 段拆成 `server/db/*.ts`，`db.ts` 留 barrel re-export。先抽 `db/connection.ts`（getDb/withRetry/isTransientDbError）+ `db/ddl.ts`（ensure*Table）再按段拆。消费方多为 `import * as db`，barrel 安全。**大但机械**，建议单独一个提交、运行机全量 `tsc`+`vitest`。

## B3 · 跨层契约收敛（沉浸/场景态真相）

- **[2] 沉浸真相只在 qq-bridge，web 入口不传 immersiveMode**（H，近 bug 级）：把 `immersiveMode = getSceneMode \|\| sceneOverlay!=null` 下沉为 social 层共享解析器，web 入口（routers.ts:445/511）激活场景时也得出正确沉浸态。验证：网页激活背景场景后，源召回正确跳过、不再误删【】旁白。
- **[1] 沉浸标志群平铺穿 4 层**（H/M）：收敛 `immersiveMode/dualMode/replyLengthOverride/keepGoing/allowPhotoIntent` 成一个 turn-context 对象。**核验提醒**：别把无法在 bridge 一处算定的不变式塞进去，避免新漂移。
- **[17] dualMode 三处重算 + 续写重入丢 keepGoing**（M/M）：最值得修的是 `message-handler.ts:520` 续写重入 `handleQqPersonaChatDetailed` 时 `continuationCue` 不含关键词 → `keepGoing` 必为 false。显式透传 keepGoing/dualMode，别靠 cue 文本旁路。

## B4 · 换人物地基（配置化，为强化拟人/第二分身铺路；中等工作量）

- **[7] buildImmersiveOverlay 写死王芃泽/敏子/原著笔法**（H/L）：把 `persona-utils.ts:96-145` 的 ~17 条沉浸铁律里的体格/称呼/地名/原著笔法专属字面量，下沉到 `persona-life-config`（配置管道已被 life-schedule/source-grounding/source-recall 消费，可用）。**这是黏性最核心提示词，改动需配快照测试**。
- **[8] 作息表 + 急事/叫醒判定写死南京-王芃泽**（H/L）：`life-schedule.ts:85-120` 三套作息字面量 + `isUrgentOrWakeMessage`/`isWakeMessage` 裸正则 → 数据驱动。最高价值是做「装配器」让 `lifeConfig.routines` 真正接上（现全仓无 persona 写过 routines，一律回退内置表）。
- **[9] 触发词/代词改写散落四文件，sourceTerms 几乎零消费**（H/M）：`source-recall.ts:11` `SOURCE_RECALL_TRIGGER`、`memory-recall.ts:50` `MEMORY_TRIGGER_PATTERN` + `applyCurrentUserPronounOverride`、`memory-governance` 各硬编码专名 → 统一读 `lifeConfig.sourceTerms`。**核验更正**：sourceTerms 真实消费点在 `db.ts:1213 COMMON_SOURCE_TERMS`，且读的是 DEFAULT 常量非 per-persona 解析值——换人物时要打通到 `getPersonaLifeConfig()`。
- **[20] buildSystemPrompt 十余块裸字面量指令**（M/M）：`persona-utils.ts:167-202` 预设「男性成年人/成熟男人/微信」的块按 persona 开关/可替换化。**核验更正**：「persona-utils 是换人物单点」不成立，同类字面量散落约 33 文件，需配套清。
- **[19] 新增主动消息类型要复制整套 setInterval+catch-up+去重**（M/L）：抽共享调度抽象；常量表（概率/配额/间隔）改走 personaData 配置（`proactive.times` 已走配置，可对齐）。

## B5 · 重复抽象归一（去重，降维护面）

- **[14] 出图两条链路各拼发送/记账/交付句**（H）：`message-handler.generateAndSendPhoto`(540) 与 `scheduled-photo.ts`(65) 统一出图出口；定时那条目前**不 recordSelfieSent、不 createMessage**，归一时补上。**核验更正**：是「两条」不是三条；proactive-delivery 只管 text/voice/sticker 无 photo。
- **[15+6] 文本链/媒体链同构编排器**（H/L）：先抽逐行相同的 helper（lifeGate 门控、pinnedFacts try/catch、内心态演进回写），再渐进收敛单管线。`socialSystemPromptOverlay` 已漂移，抽「共享骨架+各 append 专属行」别硬合。
- **[24] 两个主动消息生成器 prompt/LLM 参数逐行复制**（M）：scheduler 与 ambient 的异地约束句字节级相同 → 抽共享 helper（route 后缀入参）。
- **[23] 三套召回-注入无统一 Recaller 契约**（M）：source-recall/source-grounding/memory-recall 统一签名 + 触发词单一来源。先接 lifeConfig 再统一契约。
- **[25] compact/STOP_WORDS/termsFrom/localDateKey 八文件各写一份**（M/S）：合并前**按差异分级**——`conversation-continuity:21` 的 compact 用 `.slice()` 按 UTF-16 截断，与其余四份用 `Array.from` 逐码点不同，别无脑合一。
- **[26] selfie/scheduled-photo/proactive-multimodal 概率门控各硬编码**（L/M）：抽统一决策原语 + per-persona 可配；先做值得的子集（VOICE/STICKER 概率、selfie cooldown），别一刀切大配置块。
- **[18] output-diagnostics 表清单是 db.ts 第2/3份手抄**（M）：✅**export/delete 两组分段已去重落地**（见上「已落地」段）。**待做**：`PLAN2_PERSISTENT_TABLES`（db.ts 无对应导出，需先在 db.ts 抽 canonical）+ 去硬编码路径（`localWorktree:'F:/Code/Mirrai'` 等）。

## ⚠️ 方向敏感（做之前先确认）

- **[21] 每条消息 4-5 次串行 LLM**（M/L）：reflection + 语音判定 + 源接地改写 + 主回合。**核验明确：砍掉 reflection「隐藏思考层」会削弱拟人感，那个子项必须剔除**。可做的只是并行化/按需触发（如非源召回轮不跑 grounding 改写），**不是减层**。

---

## 驳回的 9 条（已核验为「真实但只是行级小修」或证伪，**不必做**）

- `_core → social/photo-intent` 反向依赖：真实但仅消费一个布尔，行级，非框架级。
- `db.ts → social/roleplay-channel-policy` 反向依赖：仅 3 行纯函数，行级。
- 平台/渠道写死 union 难扩展：**证伪**——无 platform switch，加渠道改动面小。
- selfie-provider 残留 `buildSelfiePrompt/buildEnvironmentPrompt`：真死代码但已在 03_索引 记「待清理」，行级删除即可。
- proactive-multimodal 头注释「发送是后续工作」过期：仅注释漂移，改注释即可。
- `qqAutoBindSingleReadyPersona` 默认开：是行为决策非框架，留给产品判断。
- 三条主动线每 60s 全表扫 personas：真实但当前 persona 量级下非瓶颈，行级加 SQL 谓词即可。
- life config / persona profile 每回合重算无缓存：**证伪**——纯函数微秒级，缓存无收益。
- runProactiveTick 同 tick 多次 updatePersona：真实但 last-write-wins 无 bug，行级合并。
